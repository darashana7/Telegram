[build-system]
requires = ["setuptools", "wheel"]

[project]
name = "minervini-screener"
version = "2.0.0"
description = "Minervini Stock Screener Bot"
requires-python = ">=3.9"

[tool.railway]
start = "python render_bot.py"
